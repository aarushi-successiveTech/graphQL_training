type User {
    id: ID!
    name: String!
    email: String!
}

type Post {
    id: ID!
    title: String!
    author: User!
    comments: [Comment!]!
}

type Comment{
    id: ID!
    content: String!
    post: Post!
}